package hr.fer.zemris.java.hw06.shell;

/**
 * Enum that determines if shell should be terminated.
 * 
 * @author Luka Grgić
 * @version 1.0
 */
public enum ShellStatus {
	
	/** If shell should not be terminated. */
	CONTINUE,
	/** If shell should be terminated. */
	TERMINATE

}
